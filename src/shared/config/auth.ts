
/** How long before expiry (ms) to trigger a token refresh */
export const TOKEN_REFRESH_THRESHOLD = 2 * 60 * 1000  // 2 minutes

/** How often (ms) to check token expiry */
export const TOKEN_REFRESH_INTERVAL = 60 * 1000        // 1 minute
